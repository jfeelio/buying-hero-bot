# Carrot Dashboard Login
CARROT_EMAIL=you@example.com
CARROT_PASSWORD=yourpassword

# Anthropic (Claude AI)
ANTHROPIC_API_KEY=sk-ant-...

# Google PageSpeed Insights (free key — enable "PageSpeed Insights API" in Google Cloud)
PAGESPEED_API_KEY=AIza...

# Google Sheets/Docs (reuse existing service account credentials.json)
GOOGLE_CREDS_PATH=../foreclosure-agent/credentials.json
SEO_GOOGLE_SHEET_ID=

# Site to audit
TARGET_URL=https://www.buyinghero.com

# Max pages to crawl (default: 50)
MAX_CRAWL_PAGES=50
